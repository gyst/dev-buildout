[buildout]
extensions = mr.developer
unzip = true
parts =
    var
    karl
    karlserve
    postoffice

index = http://karlhosting.github.com/pyramid/production/index
find-links = http://karlhosting.github.com/pyramid/staging/index
# override any eggs-directory or download-cache settings
# that might be in ~/.buildout/default.cfg. We have to do this because
# we're pinning versions using a custom package index instead of
# using versions.cfg.
eggs-directory = ${buildout:directory}/eggs
download-cache =

customization-packages =
    osi
    eurasia
    oxfam
    ospc
#    ariadne
#    fahamu
#    gcfe
#    trustafrica

[var]
recipe = z3c.recipe.mkdir
paths =
    var/log

[sources]
karl = git git@github.com:karlproject/karl.git branch=pyramid
karlserve = git git@github.com:karlproject/karlserve.git branch=pyramid
osi = git git@github.com:karlhosting/osi.git branch=pyramid
eurasia = git git@github.com:karlhosting/eurasia.git branch=pyramid
ospc = git git@github.com:karlhosting/ospc.git branch=pyramid
oxfam = git git@github.com:karlhosting/oxfam.git branch=pyramid
ariadne = svn https://svn.sixfeetup.com/svn/karl/ariadne/karl3-buildout/trunk/src/ariadne
relstorage = svn svn://svn.zope.org/repos/main/relstorage/trunk
repoze.pgtextindex = svn svn+ssh://repoze@svn.repoze.org/svn/repoze.pgtextindex/trunk
repoze.postoffice = svn svn+ssh://repoze@svn.repoze.org/svn/repoze.postoffice/trunk
repoze.sendmail = git git@github.com:repoze/repoze.sendmail.git
repoze.urchin = svn svn+ssh://repoze@svn.repoze.org/svn/repoze.urchin/trunk
repozitory = git git://github.com/hathawsh/repozitory.git

[karl]
# We install the karl egg separately to avoid installing all of core
# Karl's console scripts, since karlserve replaces those and we don't
# need confusing artifacts floating around in bin/.
recipe = zc.recipe.egg
unzip = true
eggs =
       karl
scripts = juice_all

[karlserve]
recipe = zc.recipe.egg
unzip = true
dependent-scripts = true
eggs =
    ${buildout:customization-packages}
    repoze.debug
    supervisor
    karlserve
    nose
    coverage
    guillotine
    RelStorage
    repoze.pgtextindex
    repozitory

dependent-scripts = true
scripts =
    supervisord = supervisord
    supervisorctl = supervisorctl
    karlserve = karlserve
    nosetests = nosetests
    runzeo = runzeo
    zeopack = zeopack
    paster = paster

interpreter = py

[postoffice]
recipe = zc.recipe.egg
unzip = true
dependent-scripts = true
eggs =
    repoze.postoffice
interpreter = pypo

scripts =
    postoffice
    po_debug
